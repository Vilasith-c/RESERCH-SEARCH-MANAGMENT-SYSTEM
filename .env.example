# PostgreSQL Configuration for Recursive Influence Tracker
# Optional: The web app automatically runs in Smart Search (In-Memory CSV) mode if PostgreSQL is not connected.
PGHOST=localhost
PGPORT=5432
PGDATABASE=research_db
PGUSER=postgres
PGPASSWORD=
